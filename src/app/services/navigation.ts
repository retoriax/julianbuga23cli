/**
 * Enum for all possible navigation options in the app.
 */
export enum Navigation {
  Blank = "/blank",
  RoutePlanner = "/route-planner",
  Map = "/map",
  Help = "/help",
  Login = '/login',
  Register = "/register"
}
