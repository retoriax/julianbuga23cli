import { LoginStatusrequest } from './login-statusrequest';

describe('LoginStatusrequest', () => {
  it('should create an instance', () => {
    expect(new LoginStatusrequest()).toBeTruthy();
  });
});
